#include <Geode/Geode.hpp>

using namespace geode::prelude;

// thank u abb2k on github
class Settings {
public:
	static int defaultPct();
	static std::string defaultWebhook();
	static std::string displayName();
};